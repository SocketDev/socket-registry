# Node.js compile cache directory for faster startup
NODE_COMPILE_CACHE="./.cache"

# Pre-commit environment marker (set by git hooks)
PRE_COMMIT=""

# External dependency linting flag (set when linting external packages)
LINT_EXTERNAL=""
