name: 'Setup'
description: 'Setup debug, Node.js, pnpm, Socket firewall shims, and bootstrap zero-dep Socket packages (@socketsecurity/lib, @socketregistry/packageurl-js, @sinclair/typebox)'

# Dependencies:
#   - actions/setup-node@v6.2.0
#   - SocketDev/socket-registry/.github/actions/debug

inputs:
  debug:
    description: 'Enable debug output'
    required: false
    default: '0'
  node-version:
    description: 'Node.js version'
    required: false
    default: '25.9.0'
  scope:
    description: 'npm registry scope for package authentication'
    required: false
    default: ''
  registry-url:
    description: 'npm registry URL'
    required: false
    default: 'https://registry.npmjs.org'
  socket-api-key:
    description: 'Socket API key — when provided, uses sfw-enterprise instead of sfw-free'
    required: false
    default: ''
  working-directory:
    description: 'Working directory'
    required: false
    default: '.'

runs:
  using: 'composite'
  steps:
    - name: Setup debug
      uses: SocketDev/socket-registry/.github/actions/debug@906cdd95b4df2231f1c7a721ddaf55a43349e280 # main
      with:
        debug: ${{ inputs.debug }}

    - name: Install pnpm
      shell: bash
      run: | # zizmor: ignore[github-env]
        set -euo pipefail
        # Single source of truth: external-tools.json at the repo root of this action's checkout.
        TOOLS_FILE="${GITHUB_ACTION_PATH}/../../../external-tools.json"
        if [ ! -f "$TOOLS_FILE" ]; then
          echo "× socket-registry setup action is broken: external-tools.json not found at ${TOOLS_FILE}." >&2
          echo "  This is a packaging bug in SocketDev/socket-registry, not a consumer issue. File a bug." >&2
          exit 1
        fi
        # Copy the tools file to RUNNER_TEMP so consumer workflows and
        # Docker builds can reach it after `setup` finishes. Actions
        # lose their $GITHUB_ACTION_PATH scope between steps, so we
        # stash the file at a stable location and export the path via
        # SOCKET_TOOL_CHECKSUMS_FILE. Dockerfiles that verify any
        # registry-published tool (pnpm, zizmor, …) can `COPY` this
        # into their build context without duplicating per-platform
        # SHAs across each Dockerfile.
        TOOLS_DEST="${RUNNER_TEMP:-/tmp}/socket-registry-external-tools.json"
        cp "$TOOLS_FILE" "$TOOLS_DEST"
        if [ ! -s "$TOOLS_DEST" ]; then
          echo "× failed to stage external-tools.json at ${TOOLS_DEST} (missing or empty after copy)." >&2
          exit 1
        fi
        echo "SOCKET_TOOL_CHECKSUMS_FILE=$TOOLS_DEST" >> "${GITHUB_ENV:-/dev/null}"
        # Read JSON values via lib/jq.mjs. Node is preinstalled on
        # every GitHub-hosted runner image and in node:* Docker base
        # images, so this avoids depending on jq (absent from
        # node:*-alpine and distroless). The reader exits non-zero on
        # missing/empty values, so set -e turns a packaging bug here
        # into a loud failure rather than a silent empty env export
        # that surfaces later as `cp: cannot stat ''`.
        JQ="${GITHUB_ACTION_PATH}/../lib/jq.mjs"
        PNPM_VERSION="$(node "$JQ" "$TOOLS_FILE" pnpm version)"
        PNPM_DIR="${RUNNER_TEMP:-/tmp}/pnpm-bin"
        # Canonical platform string (linux-x64, linux-arm64-musl,
        # darwin-arm64, win-x64, …). Detects musl via Node's own
        # process.report so we don't shell out to ldd.
        PLATFORM_TOOL="${GITHUB_ACTION_PATH}/../lib/platform.mjs"
        PLATFORM="$(node "$PLATFORM_TOOL")"
        ASSET="$(node "$JQ" "$TOOLS_FILE" pnpm checksums "$PLATFORM" asset)"
        EXPECTED_SHA256="$(node "$JQ" "$TOOLS_FILE" pnpm checksums "$PLATFORM" sha256)"
        PNPM_BIN="$PNPM_DIR/pnpm"
        [[ "$ASSET" == *.zip ]] && PNPM_BIN="$PNPM_DIR/pnpm.exe"
        # Shared installer: fetches, verifies sha256, extracts, chmods.
        INSTALL_TOOL="${GITHUB_ACTION_PATH}/../lib/install-tool.mjs"
        if [ ! -x "$PNPM_BIN" ]; then
          node "$INSTALL_TOOL" \
            "https://github.com/pnpm/pnpm/releases/download/v${PNPM_VERSION}/${ASSET}" \
            "$EXPECTED_SHA256" \
            "$PNPM_DIR"
        fi
        echo "$PNPM_DIR" >> "${GITHUB_PATH:-/dev/null}"
        # Export canonical pnpm provenance for downstream steps and
        # Docker builds — lets consumers pass `--build-arg
        # PNPM_VERSION=${{ env.SOCKET_TOOL_PNPM_VERSION }}` instead of
        # hardcoding a drift-prone ARG default.
        {
          echo "SOCKET_TOOL_PNPM_VERSION=$PNPM_VERSION"
          echo "SOCKET_TOOL_PNPM_PLATFORM=$PLATFORM"
          echo "SOCKET_TOOL_PNPM_ASSET=$ASSET"
          echo "SOCKET_TOOL_PNPM_SHA256=$EXPECTED_SHA256"
          echo "SOCKET_TOOL_PNPM_BIN=$PNPM_BIN"
          echo "SOCKET_TOOL_PNPM_DIR=$PNPM_DIR"
        } >> "${GITHUB_ENV:-/dev/null}"

    - name: Setup Node.js
      uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0
      with:
        node-version: ${{ inputs.node-version }}
        registry-url: ${{ inputs.registry-url }}
        scope: ${{ inputs.scope }}

    - name: Export Node.js version
      shell: bash
      env:
        # Route through env to avoid zizmor template-injection findings —
        # the input is still plumbed from the caller but never expanded
        # directly into the shell source.
        NODE_VERSION: ${{ inputs.node-version }}
      run: | # zizmor: ignore[github-env]
        # Mirror the node version that setup-node resolved so Docker
        # builds COPY'd from CI can pin to the same Node major.
        echo "SOCKET_TOOL_NODE_VERSION=$NODE_VERSION" >> "${GITHUB_ENV:-/dev/null}"

    - name: Download sfw
      shell: bash
      env:
        GITHUB_TOKEN: ${{ github.token }}
        SOCKET_API_KEY: ${{ inputs.socket-api-key }}
      run: | # zizmor: ignore[github-env]
        set -euo pipefail
        # SFW (Socket Firewall) version + per-platform asset/sha256
        # live in external-tools.json under sfw.<flavor>.checksums.
        # Bumping a tool requires updating the version AND every
        # platform's sha256 there in the same commit. The lib/ scripts
        # below resolve platform → asset → URL → install at the
        # currently-detected runner.
        TOOLS_FILE="${GITHUB_ACTION_PATH}/../../../external-tools.json"
        JQ="${GITHUB_ACTION_PATH}/../lib/jq.mjs"
        PLATFORM_TOOL="${GITHUB_ACTION_PATH}/../lib/platform.mjs"
        INSTALL_TOOL="${GITHUB_ACTION_PATH}/../lib/install-tool.mjs"
        # Enterprise flavor when SOCKET_API_KEY is set, otherwise free.
        # Enterprise downloads from the private firewall-release repo
        # are auth'd via GITHUB_TOKEN inside install-tool.mjs.
        if [ -n "$SOCKET_API_KEY" ]; then
          SFW_FLAVOR="enterprise"
          SFW_REPO="SocketDev/firewall-release"
        else
          SFW_FLAVOR="free"
          SFW_REPO="SocketDev/sfw-free"
        fi
        SFW_VERSION="$(node "$JQ" "$TOOLS_FILE" sfw version)"
        PLATFORM="$(node "$PLATFORM_TOOL")"
        # Hard-fail with a clear message on unsupported platforms (eg
        # win-arm64) rather than silently skipping. SFW is a required
        # dependency of the install flow, unlike zizmor (audit-only).
        if ! ASSET="$(node "$JQ" "$TOOLS_FILE" sfw "$SFW_FLAVOR" checksums "$PLATFORM" asset 2>/dev/null)"; then
          echo "× SFW (${SFW_FLAVOR}) is not published for ${PLATFORM} at v${SFW_VERSION}." >&2
          echo "  Supported: linux-{x64,arm64}{,-musl}, darwin-{x64,arm64}, win-x64." >&2
          echo "  win-arm64 has no upstream binary — skip SFW-dependent steps on that runner or wait for upstream support." >&2
          exit 1
        fi
        EXPECTED_SHA256="$(node "$JQ" "$TOOLS_FILE" sfw "$SFW_FLAVOR" checksums "$PLATFORM" sha256)"
        SFW_BIN_NAME="$(node "$JQ" "$TOOLS_FILE" sfw "$SFW_FLAVOR" binaryName)"
        if [[ "$ASSET" == *.exe ]]; then
          SFW_BIN_NAME="${SFW_BIN_NAME}.exe"
        fi
        SFW_DIR="${RUNNER_TEMP:-/tmp}/sfw-bin"
        SFW_BIN="$SFW_DIR/$SFW_BIN_NAME"
        if [ ! -x "$SFW_BIN" ]; then
          node "$INSTALL_TOOL" \
            "https://github.com/${SFW_REPO}/releases/download/v${SFW_VERSION}/${ASSET}" \
            "$EXPECTED_SHA256" \
            "$SFW_DIR" \
            "$SFW_BIN_NAME"
        fi
        # Export canonical sfw provenance in the shared SOCKET_TOOL_*
        # namespace. `SOCKET_TOOL_SFW_FLAVOR` tells consumers which
        # release repo was used without re-reading SFW_IS_ENTERPRISE.
        SFW_IS_ENTERPRISE=false
        if [ "$SFW_FLAVOR" = "enterprise" ]; then
          SFW_IS_ENTERPRISE=true
          echo "SOCKET_API_KEY=$SOCKET_API_KEY" >> "${GITHUB_ENV:-/dev/null}"
        fi
        {
          echo "SFW_BIN=$SFW_BIN"
          echo "SFW_IS_ENTERPRISE=$SFW_IS_ENTERPRISE"
          echo "SOCKET_TOOL_SFW_FLAVOR=$SFW_FLAVOR"
          echo "SOCKET_TOOL_SFW_VERSION=$SFW_VERSION"
          echo "SOCKET_TOOL_SFW_PLATFORM=$PLATFORM"
          echo "SOCKET_TOOL_SFW_ASSET=$ASSET"
          echo "SOCKET_TOOL_SFW_SHA256=$EXPECTED_SHA256"
        } >> "${GITHUB_ENV:-/dev/null}"

    - name: Create sfw shims
      shell: bash
      run: | # zizmor: ignore[github-env]
        # Shim supported package managers so their commands route through sfw.
        #
        # Wrapper mode ecosystems (sfw-free):
        #   JavaScript/TypeScript: npm, yarn, pnpm
        #   Python: pip, uv
        #   Rust: cargo
        # https://github.com/SocketDev/sfw-free?tab=readme-ov-file#supported-package-managers
        #
        # Additional wrapper mode ecosystems (sfw-enterprise):
        #   Ruby: gem, bundler
        #   .NET: nuget
        #   Go: go (Linux only)
        # https://github.com/SocketDev/firewall-release/wiki#support-matrix
        SHIM_DIR="${RUNNER_TEMP:-/tmp}/sfw-shim"
        rm -rf "$SHIM_DIR"
        mkdir -p "$SHIM_DIR"
        IS_WINDOWS=false
        [[ "$OSTYPE" == msys* || "$OSTYPE" == cygwin* ]] && IS_WINDOWS=true
        msys_to_win_path() {
          if $IS_WINDOWS && [[ "$1" =~ ^/([a-zA-Z])/(.*) ]]; then
            echo "${BASH_REMATCH[1]^^}:\\${BASH_REMATCH[2]//\//\\}"
          else
            echo "$1"
          fi
        }
        strip_shim_dir() { echo "$PATH" | tr ':' '\n' | grep -vxF "$SHIM_DIR" | paste -sd: -; }
        CLEAN_PATH="$(strip_shim_dir)"
        SSL_WORKAROUND=""
        SHIM_CMDS="npm yarn pnpm pip pip3 uv cargo"
        if [ "$SFW_IS_ENTERPRISE" = "true" ]; then
          SHIM_CMDS="npm yarn pnpm pip pip3 uv cargo gem bundler nuget"
          # Go wrapper mode is only supported on Linux.
          [[ "$OSTYPE" == linux* ]] && SHIM_CMDS="$SHIM_CMDS go"
        else
          SSL_WORKAROUND='export GIT_SSL_NO_VERIFY=true # Workaround: sfw-free does not yet set GIT_SSL_CAINFO.'
        fi
        for CMD in $SHIM_CMDS; do
          REAL="$(PATH="$CLEAN_PATH" command -v "$CMD" 2>/dev/null || true)"
          [ -z "$REAL" ] && continue
          REAL="$(msys_to_win_path "$REAL")"
          SHIM_LINES=('#!/bin/bash' "export PATH=\"\$(echo \"\$PATH\" | tr ':' '\n' | grep -vxF '${SHIM_DIR}' | paste -sd: -)\"")
          [ -n "$SSL_WORKAROUND" ] && SHIM_LINES+=("$SSL_WORKAROUND")
          SHIM_LINES+=("exec \"${SFW_BIN}\" \"${REAL}\" \"\$@\"")
          printf '%s\n' "${SHIM_LINES[@]}" > "$SHIM_DIR/$CMD"
          chmod +x "$SHIM_DIR/$CMD"
          if $IS_WINDOWS; then
            printf '@echo off\r\nset "PATH=;%%PATH%%;"\r\nset "PATH=%%PATH:;%s;=;%%"\r\nset "PATH=%%PATH:~1,-1%%"\r\n"%s" "%s" %%*\r\n' \
              "$SHIM_DIR" "$SFW_BIN" "$REAL" > "$SHIM_DIR/$CMD.cmd"
          fi
        done
        echo "$SHIM_DIR" >> "${GITHUB_PATH:-/dev/null}"
        echo "SFW_SHIM_DIR=$SHIM_DIR" >> "${GITHUB_ENV:-/dev/null}"

    - name: Export SFW_CUSTOM_REGISTRIES bypass list
      shell: bash
      run: | # zizmor: ignore[github-env]
        # Ship an up-to-date host bypass list to both sfw-free and
        # sfw-enterprise via SFW_CUSTOM_REGISTRIES, so consumers pick up
        # new hosts without waiting for a binary release.
        # Kinds accepted: npm, pypi, golang, maven, gem, cargo, nuget,
        # block, wrap, bypass.
        # Format: comma- or newline-separated kind:fqdn entries.
        # Source of truth (baked into the binary) lives at
        # SocketDev/firewall:src/lib/registries/default.ts.
        {
          echo 'SFW_CUSTOM_REGISTRIES<<EOF'
          # GitHub release-asset CDNs (targets of github.com/*/releases/download/*
          # and github.com/*/archive/* redirects).
          echo 'bypass:objects.githubusercontent.com'
          echo 'bypass:release-assets.githubusercontent.com'
          echo 'bypass:raw.githubusercontent.com'
          echo 'bypass:gist.githubusercontent.com'
          # VCS fallbacks (Go modules, npm git-deps, Ruby git sources).
          echo 'bypass:gitlab.com'
          echo 'bypass:bitbucket.org'
          # Build-tool toolchain downloads.
          echo 'bypass:ziglang.org'
          echo 'bypass:cmake.org'
          echo 'bypass:sh.rustup.rs'
          echo 'bypass:nodejs.org'
          echo 'bypass:bootstrap.pypa.io'
          # Go module proxy + toolchain auto-download. proxy.golang.org
          # serves modules and 302s to storage.googleapis.com for the
          # `go: downloading go1.x.y` flow; sum.golang.org is the
          # checksum DB. Without these, `go build` fails with
          # `tls: failed to verify certificate: x509: certificate
          # signed by unknown authority` because SFW's MITM breaks
          # the cert chain at the redirect target.
          echo 'bypass:proxy.golang.org'
          echo 'bypass:sum.golang.org'
          echo 'bypass:storage.googleapis.com'
          # Cargo web portal (search/info API).
          echo 'bypass:crates.io'
          # Conda ecosystem.
          echo 'bypass:repo.anaconda.com'
          echo 'bypass:conda.anaconda.org'
          echo 'bypass:anaconda.org'
          echo 'bypass:conda-forge.org'
          # Composer (PHP).
          echo 'bypass:packagist.org'
          echo 'bypass:repo.packagist.org'
          # Conan (C/C++).
          echo 'bypass:center.conan.io'
          echo 'bypass:conan.io'
          # CocoaPods (Swift/Obj-C).
          echo 'bypass:cdn.cocoapods.org'
          # Hackage (Haskell).
          echo 'bypass:hackage.haskell.org'
          # Hex (Elixir/Erlang).
          echo 'bypass:hex.pm'
          echo 'bypass:repo.hex.pm'
          echo 'bypass:builds.hex.pm'
          # HuggingFace model hub.
          echo 'bypass:huggingface.co'
          # Julia.
          echo 'bypass:pkg.julialang.org'
          echo 'bypass:julialang-s3.julialang.org'
          # LuaRocks.
          echo 'bypass:luarocks.org'
          # OPAM + OCaml toolchain.
          echo 'bypass:opam.ocaml.org'
          echo 'bypass:ocaml.org'
          # pub.dev (Dart / Flutter).
          echo 'bypass:pub.dev'
          # CPAN (Perl).
          echo 'bypass:metacpan.org'
          echo 'bypass:cpan.metacpan.org'
          # CRAN (R).
          echo 'bypass:cran.r-project.org'
          echo 'bypass:cran.rstudio.com'
          echo 'bypass:bioconductor.org'
          # Bitnami.
          echo 'bypass:downloads.bitnami.com'
          # Swift toolchain + package index.
          echo 'bypass:swift.org'
          echo 'bypass:download.swift.org'
          echo 'bypass:swiftpackageindex.com'
          # VSCode extension marketplace.
          echo 'bypass:marketplace.visualstudio.com'
          # Bazel external workspace mirror.
          echo 'bypass:mirror.bazel.build'
          echo 'EOF'
        } >> "${GITHUB_ENV:-/dev/null}"
        # Opt out of gh CLI telemetry (on-by-default as of late 2025).
        # DO_NOT_TRACK covers tools honoring consoledonottrack.com;
        # GH_TELEMETRY is gh-specific and takes precedence.
        echo "DO_NOT_TRACK=1" >> "${GITHUB_ENV:-/dev/null}"
        echo "GH_TELEMETRY=0" >> "${GITHUB_ENV:-/dev/null}"

    - name: Bootstrap zero-dep packages from npm registry
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        ACTION_DIR: ${{ github.action_path }}
      run: |
        # Bootstrap a small set of zero-dep packages into node_modules/
        # BEFORE pnpm install runs. Several Socket repos have root
        # scripts (setup.mts, postinstall hooks, .claude/hooks/*) that
        # import these packages at module-load time. On a fresh
        # checkout, pnpm install itself runs scripts that import them —
        # but pnpm install hasn't completed yet, so the imports fail
        # with ERR_MODULE_NOT_FOUND. Pre-seeding each package's tarball
        # directly into node_modules/<scope>/<name>/ solves the
        # chicken-and-egg.
        #
        # Restricted to ZERO-DEP packages so a tarball extract is the
        # whole install — no transitive resolution needed. Any package
        # we add here must `npm view <pkg> dependencies` empty; if it
        # picks up a dep later, drop it from this list.
        #
        # Lives in `setup` (alongside zizmor + sfw provisioning) rather
        # than `install` so consumers calling only `setup` also benefit,
        # not just consumers of `setup-and-install` or `install`
        # directly.
        #
        # Per-package skip rules:
        #   - the repo has its own scripts/bootstrap-from-registry.mts
        #     (the preinstall hook handles all packages; no duplicate)
        #   - <pkg> is already resolvable from the working directory
        #   - <pkg> isn't pinned in this repo's package.json /
        #     pnpm-workspace.yaml catalog (skip silently)
        #
        # Before each download, the version is checked against the
        # Socket Firewall API (firewall-api.socket.dev/purl). Critical/
        # high severity alerts block the bootstrap; firewall errors
        # are non-fatal so a network blip doesn't break a fresh clone.
        set -euo pipefail
        if [ -f scripts/bootstrap-from-registry.mts ]; then
          echo "Repo has its own bootstrap-from-registry.mts; skipping action-level bootstrap."
          exit 0
        fi
        # Zero-dep packages worth pre-seeding so .claude/hooks/* and
        # root scripts can import them at module-load time. Order
        # matters only for log readability.
        PACKAGES=(
          '@socketsecurity/lib'
          '@socketregistry/packageurl-js'
          '@sinclair/typebox'
        )
        for PKG in "${PACKAGES[@]}"; do
          # Already resolvable? Skip.
          if node -e "require.resolve('${PKG}/package.json')" 2>/dev/null; then
            echo "${PKG} already resolvable; skipping."
            continue
          fi
          # Pinned somewhere in the consumer repo?
          VERSION="$(node "${ACTION_DIR}/../lib/read-pinned-version.mts" "${PKG}")"
          if [ -z "$VERSION" ]; then
            echo "${PKG} not pinned in this repo; skipping."
            continue
          fi
          # Firewall check — bail loudly on critical/high alerts.
          if ! node "${ACTION_DIR}/../lib/check-firewall.mts" "${PKG}" "$VERSION"; then
            exit 1
          fi
          # Strip leading scope from package name for tarball path.
          # `@scope/name` → `name`; `name` → `name`.
          BASE="${PKG##*/}"
          echo "Bootstrapping ${PKG}@${VERSION} from npm registry..."
          TARBALL_URL="https://registry.npmjs.org/${PKG}/-/${BASE}-${VERSION}.tgz"
          TARBALL="$(mktemp -t bootstrap-XXXXXX.tgz)"
          if ! curl -fsSL "$TARBALL_URL" -o "$TARBALL"; then
            echo "Warning: failed to fetch ${TARBALL_URL}; pnpm install will resolve it." >&2
            rm -f "$TARBALL"
            continue
          fi
          DEST="node_modules/${PKG}"
          rm -rf "$DEST"
          mkdir -p "$DEST"
          tar -xzf "$TARBALL" --strip-components=1 -C "$DEST"
          rm -f "$TARBALL"
          echo "✓ ${PKG}@${VERSION} → ${DEST}"
        done
