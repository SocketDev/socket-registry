name: ⚙️ Claude Auto Review

# Dependencies:
#   - SocketDev/socket-registry/.github/workflows/claude-auto-review.yml

on:
  pull_request:
    types: [opened]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pull-requests: read

jobs:
  auto-review:
    uses: SocketDev/socket-registry/.github/workflows/claude-auto-review.yml@ac25a346bed577db20cfd234cc8c84dfe6806a8f # main
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
