name: 🤖 Claude Auto Review

# Dependencies:
#   - SocketDev/socket-registry/.github/workflows/claude-auto-review.yml

on:
  pull_request:
    types: [opened]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pull-requests: read

jobs:
  auto-review:
    uses: SocketDev/socket-registry/.github/workflows/claude-auto-review.yml@f694b7ecc21bc3eb9c9092881ed6e7ba5ba042cc # main
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
