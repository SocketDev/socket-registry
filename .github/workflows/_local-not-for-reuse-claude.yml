name: ⚙️ Claude Code

# Dependencies:
#   - SocketDev/socket-registry/.github/workflows/claude.yml

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  issues: write
  pull-requests: write

jobs:
  claude:
    uses: SocketDev/socket-registry/.github/workflows/claude.yml@ac25a346bed577db20cfd234cc8c84dfe6806a8f # main
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
