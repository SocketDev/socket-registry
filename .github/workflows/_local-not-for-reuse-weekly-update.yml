name: 🔄 Weekly Update

# Local (socket-registry) weekly-update workflow.
# Delegates to the shared reusable at
# SocketDev/socket-registry/.github/workflows/weekly-update.yml.

on:
  schedule:
    # Run weekly on Monday at 9 AM UTC.
    - cron: '0 9 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  weekly-update:
    uses: SocketDev/socket-registry/.github/workflows/weekly-update.yml@f8100a9981bf5ca60b62b3a06a6a93a29067619b # main (2026-05-15)
    with:
      test-setup-script: ''
      test-script: 'pnpm test'
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      BOT_GPG_PRIVATE_KEY: ${{ secrets.BOT_GPG_PRIVATE_KEY }}
      SOCKET_API_TOKEN: ${{ secrets.SOCKET_API_TOKEN }}
