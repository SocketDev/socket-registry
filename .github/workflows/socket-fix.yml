name: Socket Fix

on:
  schedule:
    - cron: '0 0 * * *'   # Run daily at midnight UTC
    - cron: '0 12 * * *'  # Run daily at noon UTC
  workflow_dispatch:
    inputs:
      debug:
        description: 'Enable debug output'
        required: false
        default: '0'
        type: string
        options:
          - '0'
          - '1'

jobs:
  fix:
    uses: SocketDev/socket-registry/.github/actions/workflows/socket-fix.yml@main
    with:
      debug: ${{ inputs.debug }}
      node-version: '22'
      autopilot: true
    secrets:
      socket_cli_api_token: ${{ secrets.SOCKET_CLI_API_TOKEN }}
      github_token: ${{ secrets.GITHUB_TOKEN }}
