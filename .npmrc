# Security: Use allowlist for build scripts while allowing binary linking
# This prevents arbitrary code execution during install while keeping developer workflow intact
#
# onlyBuiltDependencies: Only these packages are allowed to run install/build scripts
# All other packages will have scripts ignored but binaries will still be linked
# (socket-registry currently has no dependencies requiring build scripts)

# Enable pre/post scripts for the main project (e.g., prepare -> husky)
enable-pre-post-scripts=true

# Root-level pnpm configuration.
auto-install-peers=false
shamefully-hoist=true