declare const isArray: typeof Array.isArray
// The runtime serves default and named per the vendored upstream contract.
// oxlint-disable-next-line socket/no-default-export -- vendored
export default isArray
export { isArray }
