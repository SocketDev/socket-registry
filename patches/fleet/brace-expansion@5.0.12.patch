diff --git a/dist/commonjs/index.js b/dist/commonjs/index.js
index 48cf0d3..650253b 100644
--- a/dist/commonjs/index.js
+++ b/dist/commonjs/index.js
@@ -330,4 +330,8 @@ function expand_(str, max, maxLength, maxDepth, depth, maxRewrites, isTop) {
     }
     return acc;
 }
+// Interop: 5.0.7 dropped the CJS default export; minimatch@9's compiled
+// __importDefault call reads `.default` (the __esModule flag skips the
+// namespace fallback), so restore it pointing at expand.
+exports.default = expand;
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/dist/esm/index.js b/dist/esm/index.js
index 0e0cc96..a9332ae 100644
--- a/dist/esm/index.js
+++ b/dist/esm/index.js
@@ -326,4 +326,9 @@ function expand_(str, max, maxLength, maxDepth, depth, maxRewrites, isTop) {
     }
     return acc;
 }
+// Interop: the ESM build ships named exports only, mirroring the CJS half
+// this patch already fixes. A default import (the shape the fleet CI cache
+// action resolves through the `import` condition) otherwise throws
+// "does not provide an export named 'default'" and kills the job at setup.
+export default expand;
 //# sourceMappingURL=index.js.map
\ No newline at end of file
