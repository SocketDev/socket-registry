/**
 * SOCKET_CLI_API_PROXY environment variable snapshot.
 * Proxy URL for Socket CLI API requests (alternative name).
 */

import { env } from 'node:process'

export const SOCKET_CLI_API_PROXY = env['SOCKET_CLI_API_PROXY']
