/**
 * SOCKET_CLI_API_TOKEN environment variable snapshot.
 * Socket CLI API authentication token (alternative name).
 */

import { env } from 'node:process'

export const SOCKET_CLI_API_TOKEN = env['SOCKET_CLI_API_TOKEN']
