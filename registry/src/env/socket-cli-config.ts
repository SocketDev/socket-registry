/**
 * SOCKET_CLI_CONFIG environment variable snapshot.
 * Socket CLI configuration file path (alternative name).
 */

import { env } from 'node:process'

export const SOCKET_CLI_CONFIG = env['SOCKET_CLI_CONFIG']
