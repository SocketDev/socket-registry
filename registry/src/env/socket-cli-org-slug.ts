/**
 * SOCKET_CLI_ORG_SLUG environment variable snapshot.
 * Socket CLI organization slug identifier (alternative name).
 */

import { env } from 'node:process'

export const SOCKET_CLI_ORG_SLUG = env['SOCKET_CLI_ORG_SLUG']
