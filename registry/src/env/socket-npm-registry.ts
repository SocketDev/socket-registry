/**
 * SOCKET_NPM_REGISTRY environment variable snapshot.
 * Socket NPM registry URL (alternative name).
 */

import { env } from 'node:process'

export const SOCKET_NPM_REGISTRY = env['SOCKET_NPM_REGISTRY']
