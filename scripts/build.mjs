/**
 * @fileoverview Build script for socket-registry monorepo.
 */

// Socket-registry monorepo doesn't have a build step at the root level.
// Individual packages handle their own builds.
console.log('No build needed for socket-registry monorepo')
process.exitCode = 0
