/**
 * @fileoverview Taze configuration for dependency updates.
 */

export default {
  mode: 'latest',
  // Wait 7 days after a package is published before suggesting it as an update
  maturityPeriod: 7,
}
