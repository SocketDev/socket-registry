# This directory contains test packages managed by pnpm workspaces
shamefully-hoist=true
auto-install-peers=false
# Suppress warnings for test dependencies
strict-peer-dependencies=false
engine-strict=false
# Note: To suppress deprecation warnings when running pnpm install manually,
# use: pnpm install --reporter=silent or --reporter=append-only